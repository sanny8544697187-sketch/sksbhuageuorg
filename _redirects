# Exclude static assets and Netlify functions from SPA redirect
/manifest.json  /manifest.json  200
/sw.js          /sw.js          200
/icons/*        /icons/:splat   200
/.netlify/*     /.netlify/:splat  200

# SPA fallback — must be last
/*  /index.html  200
